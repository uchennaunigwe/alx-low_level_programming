#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 * is_prime_number - Entry
 * @n: operand
 * Return: Me
 */

int is_prime_number(int n)
{
	if (n % 2 == 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
