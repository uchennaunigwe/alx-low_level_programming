#include <stdlib.h>
#include <string.h>
/**
 * _strcmp - Join pointers
 * @dest: dest
 * @src: src
 * Return: joined string
 */

int _strcmp(char *dest, char *src)
{
	int ptr;

	ptr = strcmp(dest, src);
	return (ptr);
}
